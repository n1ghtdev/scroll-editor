import { css } from '@emotion/core';

const Input = css`
  border: none;
  border-radius: 5px;
  padding: 7px 5px;
  font-weight: bold;
  box-shadow: inset 1px -1px 5px rgba(0, 0, 0, 0.35);
`;

export default Input;
